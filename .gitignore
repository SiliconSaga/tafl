# Pre-write captures of the domain's full record set, written by
# scripts/update-published-ip.sh. They are the undo for a setHosts call and
# contain the live DNS inventory, so they stay local.
.dns-snapshots/
